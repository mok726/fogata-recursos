<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Rama {{ age.id }} - Fogata de Recursos</title>

  <style>

    body {
      font-family: Arial, sans-serif;
      margin: auto;
      max-width: 1200px;
      padding: 1rem;
      background: #f5f5f5;
      color: #222;
    }

    .header {
      display: flex;
      gap: 2rem;
      align-items: center;
      margin-bottom: 2rem;
      flex-wrap: wrap;
    }

    .header img {
      width: 180px;
      border-radius: 12px;
      background: white;
      padding: 1rem;
    }

    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
      gap: 1rem;
    }

    .card {
      background: white;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 2px 8px rgba(0,0,0,0.08);
    }

    .card-content {
      padding: 1rem;
    }

    .age {
      display: flex;
      gap: 0.3rem;
      flex-wrap: wrap;
      margin-top: 1rem;
    }

    .age img {
      width: 42px;
      border-radius: 4px;
    }

    a {
      text-decoration: none;
      color: inherit;
    }

  </style>
</head>

<body>

<div class="header">

  <img
    src="/imagenes/edades/{{ age.id }}.png"
    alt="Rama {{ age.id }}">

  <div>
    <h1>Rama {{ age.id }}</h1>

    <p>
      {{ age.name }}
    </p>
  </div>

</div>

<div class="grid">

{% for activity in collections.activities %}

  {% if activity.data.ageGroups and (age.id in activity.data.ageGroups) %}

    <a href="{{ activity.url }}">

      <div class="card">

        <div class="card-content">

          <h2>{{ activity.data.title }}</h2>

          {% if activity.data.description %}
            <p>{{ activity.data.description }}</p>
          {% endif %}

          <div class="age">

            {% for item in activity.data.ageGroups %}
              <img
                src="/imagenes/edades/{{ item }}.png"
                alt="Rama {{ item }}">
            {% endfor %}

          </div>

        </div>

      </div>

    </a>

  {% endif %}

{% endfor %}

</div>

</body>
</html>


